imports:
  - '../aliases/color-assignments.yml'

global:
  category: buttons
  type: all

props:
  button_color_background:
    value: '{!color_background_primary}'
    type: color
    comment: 'Example — not actually using'
